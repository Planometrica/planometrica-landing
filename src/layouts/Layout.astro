---
import '../styles/global.css'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'

interface Props {
  title: string
  description?: string
  keywords?: string
  ogImage?: string
  canonical?: string
  schema?: Record<string, unknown> | Record<string, unknown>[]
  transparentHeader?: boolean
  hideHeader?: boolean
  hideFooter?: boolean
}

const {
  title,
  description = 'Планометрика — AI-платформа проектирования индивидуальных домов. Студия для частных заказчиков, ПланоCAD для профессионалов.',
  keywords,
  ogImage = '/images/og-default.jpg',
  canonical,
  schema,
  transparentHeader = false,
  hideHeader = false,
  hideFooter = false
} = Astro.props

const schemaArray = schema ? (Array.isArray(schema) ? schema : [schema]) : []

const siteUrl = import.meta.env.PUBLIC_SITE_URL || 'https://planometrica.pro'
const canonicalUrl = canonical ? `${siteUrl}${canonical}` : Astro.url.href
const ogImageUrl = ogImage.startsWith('http') ? ogImage : `${siteUrl}${ogImage}`
const isProd = import.meta.env.PROD
---

<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    {keywords && <meta name="keywords" content={keywords} />}
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" media="print" onload="this.media='all'" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Планометрика" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:locale" content="ru_RU" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />

    <!-- Additional SEO -->
    <meta name="robots" content="index, follow" />
    <meta name="author" content="Планометрика" />

    <!-- JSON-LD Structured Data -->
    {schemaArray.map((s) => (
      <script type="application/ld+json" set:html={JSON.stringify(s)} />
    ))}
  </head>
  <body class="min-h-screen antialiased bg-white text-gray-900">
    {!hideHeader && <Header transparent={transparentHeader} />}
    <slot />
    {!hideFooter && <Footer />}

    {/* Analytics — только production build */}
    {isProd && (
      <>
        {/* Google Analytics (GA4) */}
        <script async src="https://www.googletagmanager.com/gtag/js?id=G-8F7FP3LX5P"></script>
        <script is:inline>
          window.dataLayer = window.dataLayer || [];
          function gtag(){dataLayer.push(arguments);}
          gtag('js', new Date());
          gtag('config', 'G-8F7FP3LX5P');
        </script>

        {/* Yandex.Metrika */}
        <script is:inline>
          (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
          m[i].l=1*new Date();
          for(var j=0;j<document.scripts.length;j++){if(document.scripts[j].src===r)return;}
          k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
          (window,document,'script','https://mc.yandex.ru/metrika/tag.js','ym');
          ym(104628425,'init',{clickmap:true,trackLinks:true,accurateTrackBounce:true,webvisor:true});
        </script>
        <noscript>
          <div><img src="https://mc.yandex.ru/watch/104628425" style="position:absolute;left:-9999px;" alt="" /></div>
        </noscript>
      </>
    )}
  </body>
</html>
