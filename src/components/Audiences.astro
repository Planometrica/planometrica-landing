---
// Audiences section - "Для кого Планометрика"

const audiences = [
  {
    icon: '👨‍👩‍👧‍👦',
    title: 'Частные заказчики',
    quote: 'Хочу построить дом, но не знаю с чего начать',
    benefits: [
      'Создайте проект самостоятельно за 30 минут',
      'Получите смету с реальными ценами',
      'Подайте заявку на ипотеку онлайн',
    ],
    cta: { text: 'Создать проект', href: 'https://studio.planometrica.ru' },
    gradient: 'from-blue-500/10 to-cyan-500/10',
    borderColor: 'border-blue-200',
    iconBg: 'bg-blue-100',
  },
  {
    icon: '📐',
    title: 'Архитекторы и строители',
    quote: 'Хочу проектировать быстрее и зарабатывать больше',
    benefits: [
      'AI-проверка СНиП/СП/ГОСТ в реальном времени',
      'Автоматическая подготовка документов для банков',
      'Визуализация для клиентов на первой встрече',
    ],
    cta: { text: 'Попробовать ПланоCAD', href: '/planocad' },
    gradient: 'from-emerald-500/10 to-teal-500/10',
    borderColor: 'border-emerald-200',
    iconBg: 'bg-emerald-100',
  },
  {
    icon: '🏦',
    title: 'Банки и девелоперы',
    quote: 'Хочу увеличить конверсию заявок на ипотеку ИЖС',
    benefits: [
      'White-label интеграция в ваш интерфейс',
      'Проект дома за 30 минут прямо в офисе',
      '+30% конверсия ипотечных заявок',
    ],
    cta: { text: 'Стать партнёром', href: 'mailto:partners@planometrica.pro' },
    gradient: 'from-violet-500/10 to-purple-500/10',
    borderColor: 'border-violet-200',
    iconBg: 'bg-violet-100',
  },
]
---

<section class="py-24 bg-gray-50">
  <div class="container mx-auto px-4">
    <!-- Header -->
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
        Для кого Планометрика
      </h2>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto">
        Решения для каждого участника рынка ИЖС
      </p>
    </div>

    <!-- Audience Cards -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      {audiences.map((audience) => (
        <div
          class={`relative bg-gradient-to-br ${audience.gradient} rounded-3xl p-8 border ${audience.borderColor} hover:shadow-xl transition-all duration-300 group`}
        >
          <!-- Icon -->
          <div class={`w-16 h-16 ${audience.iconBg} rounded-2xl flex items-center justify-center text-3xl mb-6 group-hover:scale-110 transition-transform duration-300`}>
            {audience.icon}
          </div>

          <!-- Title -->
          <h3 class="text-2xl font-bold text-gray-900 mb-3">
            {audience.title}
          </h3>

          <!-- Quote -->
          <p class="text-gray-600 italic mb-6 text-lg">
            "{audience.quote}"
          </p>

          <!-- Benefits -->
          <ul class="space-y-3 mb-8">
            {audience.benefits.map((benefit) => (
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 text-[#92CF93] flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                </svg>
                <span class="text-gray-700">{benefit}</span>
              </li>
            ))}
          </ul>

          <!-- CTA -->
          <a
            href={audience.cta.href}
            class="inline-flex items-center gap-2 px-6 py-3 bg-[#0A4C76] text-white font-semibold rounded-xl hover:bg-[#083d5f] transition-all duration-200 group-hover:shadow-lg"
          >
            {audience.cta.text}
            <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </a>
        </div>
      ))}
    </div>
  </div>
</section>
