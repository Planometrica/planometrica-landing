---
interface Testimonial {
  quote: string
  name: string
  role: string
  location: string
  avatar?: string
}

interface Metric {
  value: string
  label: string
}

interface Props {
  testimonials?: Testimonial[]
  metrics?: Metric[]
}

const defaultTestimonials: Testimonial[] = [
  {
    quote: 'Сделали проект дома за вечер! Раньше ждали бы месяц от архитектора. AI-планировки реально работают.',
    name: 'Алексей Петров',
    role: 'Частный застройщик',
    location: 'Москва'
  },
  {
    quote: 'Использую для предварительных концепций клиентам. Экономит кучу времени на первых этапах.',
    name: 'Мария Соколова',
    role: 'Архитектор',
    location: 'Санкт-Петербург'
  },
  {
    quote: 'СПОЗУ сделали за 10 минут. Подали на уведомление, всё приняли с первого раза.',
    name: 'Дмитрий Иванов',
    role: 'Владелец участка',
    location: 'Краснодар'
  }
]

const defaultMetrics: Metric[] = [
  { value: '2,500+', label: 'Проектов создано' },
  { value: '30 мин', label: 'Среднее время' },
  { value: '98%', label: 'Довольных клиентов' },
  { value: '15+', label: 'Регионов России' }
]

const { testimonials = defaultTestimonials, metrics = defaultMetrics } = Astro.props
---

<section class="py-20 lg:py-28 bg-gradient-to-b from-white to-gray-50">
  <div class="container mx-auto px-4">
    <!-- Header -->
    <div class="text-center mb-16">
      <span class="inline-block px-4 py-2 bg-[#92CF93]/20 text-[#0A4C76] font-medium rounded-full text-sm mb-4">
        Отзывы
      </span>
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900">
        Что говорят наши пользователи
      </h2>
    </div>

    <!-- Testimonials Carousel -->
    <div class="max-w-4xl mx-auto mb-16">
      <div class="testimonials-carousel relative" data-testimonials={JSON.stringify(testimonials)}>
        <div class="overflow-hidden">
          <div class="testimonial-track flex transition-transform duration-500 ease-out">
            {testimonials.map((testimonial, index) => (
              <div class="testimonial-slide w-full flex-shrink-0 px-4" data-index={index}>
                <div class="bg-white rounded-3xl p-8 md:p-12 shadow-xl border border-gray-100">
                  {/* Quote */}
                  <div class="mb-8">
                    <svg class="w-12 h-12 text-[#92CF93] mb-6" fill="currentColor" viewBox="0 0 24 24">
                      <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z"/>
                    </svg>
                    <p class="text-xl md:text-2xl text-gray-900 leading-relaxed italic">
                      "{testimonial.quote}"
                    </p>
                  </div>

                  {/* Author */}
                  <div class="flex items-center gap-4">
                    <div class="w-14 h-14 bg-gradient-to-br from-[#0A4C76] to-[#1A7BB3] rounded-full flex items-center justify-center text-white text-xl font-bold">
                      {testimonial.name.split(' ').map(n => n[0]).join('')}
                    </div>
                    <div>
                      <p class="font-semibold text-gray-900">{testimonial.name}</p>
                      <p class="text-gray-600 text-sm">{testimonial.role}</p>
                      <p class="text-gray-500 text-sm">{testimonial.location}</p>
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Navigation */}
        <div class="flex items-center justify-center gap-4 mt-8">
          <button class="carousel-prev w-12 h-12 rounded-full bg-white shadow-md border border-gray-200 flex items-center justify-center hover:bg-gray-50 transition-colors">
            <svg class="w-5 h-5 text-gray-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
            </svg>
          </button>

          <div class="carousel-dots flex gap-2">
            {testimonials.map((_, index) => (
              <button class={`w-3 h-3 rounded-full transition-colors ${index === 0 ? 'bg-[#0A4C76]' : 'bg-gray-300'}`} data-index={index}></button>
            ))}
          </div>

          <button class="carousel-next w-12 h-12 rounded-full bg-white shadow-md border border-gray-200 flex items-center justify-center hover:bg-gray-50 transition-colors">
            <svg class="w-5 h-5 text-gray-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
            </svg>
          </button>
        </div>
      </div>
    </div>

    {/* Metrics */}
    {metrics && metrics.length > 0 && (
      <div class="max-w-4xl mx-auto">
        <div class="border-t border-gray-200 pt-12">
          <p class="text-center text-sm font-medium text-gray-500 uppercase tracking-wider mb-8">
            📊 Результаты пользователей
          </p>
          <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
            {metrics.map((metric) => (
              <div class="text-center">
                <p class="text-3xl md:text-4xl font-bold text-[#0A4C76] mb-1">{metric.value}</p>
                <p class="text-sm text-gray-600">{metric.label}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    )}
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const carousel = document.querySelector('.testimonials-carousel');
    if (!carousel) return;

    const track = carousel.querySelector('.testimonial-track') as HTMLElement;
    const slides = carousel.querySelectorAll('.testimonial-slide');
    const dots = carousel.querySelectorAll('.carousel-dots button');
    const prevBtn = carousel.querySelector('.carousel-prev');
    const nextBtn = carousel.querySelector('.carousel-next');

    let currentIndex = 0;

    function goToSlide(index: number) {
      if (index < 0) index = slides.length - 1;
      if (index >= slides.length) index = 0;

      currentIndex = index;
      track.style.transform = `translateX(-${currentIndex * 100}%)`;

      dots.forEach((dot, i) => {
        dot.classList.toggle('bg-[#0A4C76]', i === currentIndex);
        dot.classList.toggle('bg-gray-300', i !== currentIndex);
      });
    }

    prevBtn?.addEventListener('click', () => goToSlide(currentIndex - 1));
    nextBtn?.addEventListener('click', () => goToSlide(currentIndex + 1));

    dots.forEach((dot, i) => {
      dot.addEventListener('click', () => goToSlide(i));
    });

    // Auto-advance
    setInterval(() => goToSlide(currentIndex + 1), 6000);
  });
</script>
