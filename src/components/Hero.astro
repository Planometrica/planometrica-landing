---
import type { Hero as HeroType } from '../lib/sanity'

interface Props {
  hero?: HeroType | null
}

// Default content (fallback)
const defaultHero = {
  title: 'Проектирование домов',
  titleHighlight: 'с искусственным интеллектом',
  description: 'B2B платформа для архитекторов и строительных компаний. Создавайте проекты в 10 раз быстрее с помощью AI-инструментов.',
  primaryCta: { text: 'Попробовать бесплатно', link: 'https://studio.planometrica.pro' },
  secondaryCta: { text: 'Узнать больше', link: '#features' },
  stats: [
    { _key: 'stat1', value: '10x', label: 'Быстрее проектирование' },
    { _key: 'stat2', value: '500+', label: 'Готовых проектов' },
    { _key: 'stat3', value: 'AI', label: 'Powered инструменты' },
    { _key: 'stat4', value: 'B2B', label: 'Корпоративные решения' },
  ],
  gradient: 'from-[#0A4C76] via-[#1A7BB3] to-[#0A4C76]',
}

const { hero } = Astro.props
const data = hero || defaultHero

const title = data.title || defaultHero.title
const titleHighlight = data.titleHighlight || defaultHero.titleHighlight
const description = data.description || defaultHero.description
const primaryCta = data.primaryCta || defaultHero.primaryCta
const secondaryCta = data.secondaryCta || defaultHero.secondaryCta
const stats = data.stats || defaultHero.stats
const gradient = data.gradient || defaultHero.gradient
---

<section class={`relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br ${gradient}`}>
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-10">
    <div class="absolute inset-0" style="background-image: url('data:image/svg+xml,<svg width=\"60\" height=\"60\" viewBox=\"0 0 60 60\" xmlns=\"http://www.w3.org/2000/svg\"><g fill=\"none\" fill-rule=\"evenodd\"><g fill=\"%23ffffff\" fill-opacity=\"0.4\"><path d=\"M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z\"/></g></g></svg>'); background-size: 60px 60px;"></div>
  </div>

  <div class="relative z-10 container mx-auto px-4 py-20 text-center">
    <!-- Logo/Brand -->
    <div class="mb-8 flex justify-center">
      <div class="inline-flex items-center gap-3 px-6 py-3 bg-white/10 backdrop-blur-sm rounded-full border border-white/20">
        <svg class="w-8 h-8 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 12l8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25" />
        </svg>
        <span class="text-white font-semibold text-lg tracking-wide">Планометрика</span>
      </div>
    </div>

    <!-- Headline -->
    <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold text-white mb-6 tracking-tight">
      {title}
      {titleHighlight && (
        <span class="block mt-2 bg-gradient-to-r from-[#92CF93] to-[#6BB86D] bg-clip-text text-transparent">
          {titleHighlight}
        </span>
      )}
    </h1>

    <!-- Subheadline -->
    <p class="text-xl md:text-2xl text-white/80 mb-12 max-w-3xl mx-auto leading-relaxed" set:html={description} />

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center mb-16">
      {primaryCta?.text && primaryCta?.link && (
        <a
          href={primaryCta.link}
          class="inline-flex items-center justify-center px-8 py-4 text-lg font-semibold text-[#0A4C76] bg-white rounded-xl hover:bg-gray-100 transition-all duration-200 shadow-lg hover:shadow-xl hover:-translate-y-0.5"
        >
          {primaryCta.text}
          <svg class="ml-2 w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </a>
      )}
      {secondaryCta?.text && secondaryCta?.link && (
        <a
          href={secondaryCta.link}
          class="inline-flex items-center justify-center px-8 py-4 text-lg font-semibold text-white bg-white/10 backdrop-blur-sm border border-white/30 rounded-xl hover:bg-white/20 transition-all duration-200"
        >
          {secondaryCta.text}
        </a>
      )}
    </div>

    <!-- Stats -->
    {stats && stats.length > 0 && (
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 max-w-4xl mx-auto mb-16">
        {stats.map((stat) => (
          <div class="text-center">
            <div class="text-3xl md:text-4xl font-bold text-white mb-1">{stat.value}</div>
            <div class="text-white/70 text-sm">{stat.label}</div>
          </div>
        ))}
      </div>
    )}

    <!-- Partner Logos -->
    <div class="pt-8 border-t border-white/20">
      <p class="text-white/60 text-sm mb-6">Нам доверяют</p>
      <div class="flex flex-wrap items-center justify-center gap-8 md:gap-12">
        <!-- Дом.РФ -->
        <div class="flex items-center gap-2 text-white/80 hover:text-white transition-colors">
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2L2 7v10c0 5.55 3.84 10 10 10s10-4.45 10-10V7L12 2zm0 2.18l6 3.72V17c0 3.31-2.69 6-6 6s-6-2.69-6-6V7.9l6-3.72z"/>
          </svg>
          <span class="font-semibold">Дом.РФ</span>
        </div>

        <!-- Сбербанк -->
        <div class="flex items-center gap-2 text-white/80 hover:text-white transition-colors">
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10" fill="none" stroke="currentColor" stroke-width="2"/>
            <path d="M8 12h8M12 8v8"/>
          </svg>
          <span class="font-semibold">Сбербанк</span>
        </div>

        <!-- Альфа-Банк -->
        <div class="flex items-center gap-2 text-white/80 hover:text-white transition-colors">
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2L4 6v12l8 4 8-4V6l-8-4zm0 2.5L18 8v8l-6 3-6-3V8l6-3.5z"/>
          </svg>
          <span class="font-semibold">Альфа-Банк</span>
        </div>

        <!-- Россельхозбанк -->
        <div class="flex items-center gap-2 text-white/80 hover:text-white transition-colors">
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.95-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z"/>
          </svg>
          <span class="font-semibold">РСХБ</span>
        </div>

        <!-- Лемана ПРО -->
        <div class="flex items-center gap-2 text-white/80 hover:text-white transition-colors">
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-7 14l-5-5 1.41-1.41L12 14.17l4.59-4.58L18 11l-6 6z"/>
          </svg>
          <span class="font-semibold">Лемана ПРО</span>
        </div>

        <!-- ВТБ -->
        <div class="flex items-center gap-2 text-white/80 hover:text-white transition-colors">
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <rect x="3" y="6" width="18" height="12" rx="2" fill="none" stroke="currentColor" stroke-width="2"/>
            <path d="M7 12h10"/>
          </svg>
          <span class="font-semibold">ВТБ</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
    <svg class="w-6 h-6 text-white/60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>
</section>
